# AI 面试例题 · 题目清单（按原帖顺序）

来源：小红书帖子《一上午面试了六个 Agent 开发，全是半吊子》（作者：月亮向北）及评论区。
题目按原帖出现的顺序编号，原题尽量照抄。每组后面附原帖里"面试官期望听到的"，以及本仓库里对应的回答骨架、代码、测试和示例。

答题骨架统一在 [docs/00_面试问题清单与回答骨架.md](docs/00_面试问题清单与回答骨架.md)。

---

## 第一组 · 工具调用稳定性（最基础的切入点）

1. 模型调工具报错、参数不符合 schema、下游服务超时，代码层面怎么做异常处理？
   （多数回答停留在"try-except 捕获一下"，说明只跑通过正常路径。）
2. 追问：模型是直接调 API，还是先走记忆检索？
3. 追问：工具返回结果做不做格式校验？

> **期望听到**：超时重试采用指数退避策略、工具返回结果做 schema 校验、异常链路降级到人工兜底或默认返回值。这些是线上故障倒逼出来的。
>
> **本仓库**：骨架 docs/00 的 Q1–Q3 ｜ 解释 [docs/01_工具层.md](docs/01_工具层.md) ｜ 代码 `agentkit/tools/` ｜ 测试 `tests/test_tools_executor.py`、`tests/test_retry.py` ｜ 示例 `examples/01_tool_failures.py`
> 第 2 题的答案在记忆层：`agentkit/memory/manager.py::build_context`，见 [docs/02_记忆层.md](docs/02_记忆层.md) 第 2 节。

## 第二组 · 记忆系统 / 上下文管理

4. 用户对话到第二十轮，Agent 丢失了最早的关键信息，排查路径是什么？是没做记忆分层？长期记忆召回策略有问题？还是上下文窗口被塞爆了？

> **期望听到**：记忆分层、上下文压缩、关键信息优先召回。答不出这些说明只在调接口层面打转，没处理过多轮对话的信息损耗。
>
> **本仓库**：骨架 docs/00 的 Q4 ｜ 解释 [docs/02_记忆层.md](docs/02_记忆层.md) ｜ 代码 `agentkit/memory/`（排查路径本身写成了代码：`diagnose.py`）｜ 测试 `tests/test_memory.py` ｜ 示例 `examples/02_memory_turn20.py`

## 第三组 · 大模型不确定性怎么兜底

5. 大模型本身具有不确定性，Agent 行为跑偏怎么兜底？
6. 死循环场景下，模型反复调用同一个工具无法退出，是放任 Token 持续消耗，还是设置最大步数强制终止？
7. 内容安全层面，回复包含违规内容有没有审核校验？
8. 成本控制层面，多轮对话 Token 增长有没有做缓存和上下文裁剪？

> **期望听到**：一套完整的方案至少应覆盖日志重放机制、关键节点断言、Bad Case 闭环复盘。
>
> **本仓库**：骨架 docs/00 的 Q5、Q5b ｜ 解释 [docs/03_守护层.md](docs/03_守护层.md)、[docs/04_可观测性.md](docs/04_可观测性.md) ｜ 代码 `agentkit/guard/`、`agentkit/observability/`、`badcases/` ｜ 测试 `tests/test_loop_detector.py`、`tests/test_agent_guards.py`、`tests/test_guards_units.py`、`tests/test_replay_badcase.py` ｜ 示例 `examples/03_loop_guard.py`、`examples/05_incident_replay.py`

## 第四组 · 工作流编排

9. 为什么选状态机而不是线性 Chain？
10. 分支执行失败怎么回滚？

> **期望听到**：节点级可重试设计、分支判定逻辑、防止单点故障拖垮整条链路。
>
> **本仓库**：骨架 docs/00 的 Q6 ｜ 解释 [docs/05_工作流编排.md](docs/05_工作流编排.md) ｜ 代码 `agentkit/workflow/` ｜ 测试 `tests/test_workflow.py` ｜ 示例 `examples/04_workflow_refund.py`

## 第五组 · 把项目按四个维度重新拆一遍

11. 数据层：知识库怎么清洗、按什么策略切分？
12. 工具层：失败重试策略是什么，超时怎么降级？
13. 记忆层：多轮上下文怎么管理，关键信息怎么召回？
14. 工程层：Token 成本怎么控制，异常怎么收敛？

> **本仓库**：骨架 docs/00 的 Q7 ｜ 数据层 [docs/06_数据层.md](docs/06_数据层.md)（`agentkit/data/`、`tests/test_data.py`）｜ 工具层 docs/01 ｜ 记忆层 docs/02 ｜ 工程层 docs/03 + docs/04

## 评论区补充（MaxwellFDE：最能分辨的两条）

15. 讲一次真实的线上故障，从怎么发现、怎么定位到最后怎么修。（只跑通过 demo 的人讲不出"发现"那一环。）
16. 死循环那题除了步数上限，有没有做同一工具同参数连续调用的检测？（光靠步数上限，烧完预算才停。）

> **本仓库**：第 15 题 → [docs/07_线上故障复盘.md](docs/07_线上故障复盘.md)、`examples/05_incident_replay.py`、`badcases/BC-001_same_args_loop.json`（末尾有一小时演练和两分钟讲述模板）；第 16 题 → `agentkit/guard/loop_detector.py`、`tests/test_loop_detector.py`，实测数字在 [docs/03_守护层.md](docs/03_守护层.md) 第 1 节。

---

## 过题顺序建议

按 1 → 16 的顺序过，每题先自己答一遍再看 docs/00 的骨架；能不看文档复述，再去读对应代码和测试。第 15、16 题最后过，因为它们要用到前面所有的东西。
